import type { BootstrapData, LegalEntity } from "../../../../shared/types/domain";
import { CnpjInput, Select } from "../../../design-system";
import { TextField } from "../../../components/forms/LegacyFields";
import { FormGrid } from "../../../components/layout/SectionPrimitives";
import { formatCep } from "../../../../shared/utils/format";

export function LegalEntityForm({ data, form, onChange }: { data: BootstrapData; form: Omit<LegalEntity, "id" | "createdAt" | "updatedAt">; onChange: (form: Omit<LegalEntity, "id" | "createdAt" | "updatedAt">) => void }): JSX.Element {
  return <FormGrid><Select label="Organizacao" value={form.organizationId} onChange={(event) => onChange({ ...form, organizationId: event.target.value })}>{data.organizations.map((item) => <option key={item.id} value={item.id}>{item.displayName}</option>)}</Select><TextField label="Razao social" value={form.legalName} onChange={(value) => onChange({ ...form, legalName: value })} required /><TextField label="Nome fantasia" value={form.tradeName} onChange={(value) => onChange({ ...form, tradeName: value })} required /><CnpjInput label="CNPJ" value={form.cnpj ?? ""} onChange={(event) => onChange({ ...form, cnpj: event.target.value })} /><TextField label="Inscricao estadual" value={form.stateRegistration ?? ""} onChange={(value) => onChange({ ...form, stateRegistration: value || null })} /><TextField label="Email" value={form.email ?? ""} onChange={(value) => onChange({ ...form, email: value || null })} /><TextField label="Telefone" value={form.phone ?? ""} onChange={(value) => onChange({ ...form, phone: value || null })} /><TextField label="Endereco" value={form.addressLine} onChange={(value) => onChange({ ...form, addressLine: value })} required /><TextField label="Numero" value={form.addressNumber} onChange={(value) => onChange({ ...form, addressNumber: value })} required /><TextField label="Bairro" value={form.district} onChange={(value) => onChange({ ...form, district: value })} required /><TextField label="Cidade" value={form.city} onChange={(value) => onChange({ ...form, city: value })} required /><TextField label="UF" value={form.state} onChange={(value) => onChange({ ...form, state: value.toUpperCase() })} required /><TextField label="CEP" value={formatCep(form.postalCode)} onChange={(value) => onChange({ ...form, postalCode: value })} required /><TextField label="Prefixo documentos" value={form.documentPrefix ?? ""} onChange={(value) => onChange({ ...form, documentPrefix: value || null })} /><TextField label="Banco padrao fechamento" value={form.defaultBankName ?? ""} onChange={(value) => onChange({ ...form, defaultBankName: value || null })} /><TextField label="Codigo banco" value={form.defaultBankCode ?? ""} onChange={(value) => onChange({ ...form, defaultBankCode: value || null })} /><TextField label="Agencia padrao" value={form.defaultBankAgency ?? ""} onChange={(value) => onChange({ ...form, defaultBankAgency: value || null })} /><TextField label="Conta padrao" value={form.defaultBankAccount ?? ""} onChange={(value) => onChange({ ...form, defaultBankAccount: value || null })} /><TextField label="Chave PIX padrao" value={form.defaultPixKey ?? ""} onChange={(value) => onChange({ ...form, defaultPixKey: value || null })} /><label className="checkbox"><input type="checkbox" checked={form.isDraft} onChange={(event) => onChange({ ...form, isDraft: event.target.checked })} /> Rascunho</label><label className="checkbox"><input type="checkbox" checked={form.isActive} onChange={(event) => onChange({ ...form, isActive: event.target.checked })} /> Ativo</label></FormGrid>;
}
