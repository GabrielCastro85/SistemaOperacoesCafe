import type { Organization } from "../../../../shared/types/domain";
import { Select } from "../../../design-system";
import { TextField } from "../../../components/forms/LegacyFields";
import { FormGrid } from "../../../components/layout/SectionPrimitives";

export function OrganizationForm({ form, onChange }: { form: Omit<Organization, "id" | "createdAt" | "updatedAt">; onChange: (form: Omit<Organization, "id" | "createdAt" | "updatedAt">) => void }): JSX.Element {
  return <FormGrid><TextField label="Nome" value={form.name} onChange={(value) => onChange({ ...form, name: value })} required /><TextField label="Slug" value={form.slug} onChange={(value) => onChange({ ...form, slug: value })} required /><TextField label="Nome exibido" value={form.displayName} onChange={(value) => onChange({ ...form, displayName: value })} required /><TextField label="Nome do app" value={form.appDisplayName} onChange={(value) => onChange({ ...form, appDisplayName: value })} required /><TextField label="Descricao (ex: Atacado e distribuicao de cafes)" value={form.description ?? ""} onChange={(value) => onChange({ ...form, description: value || null })} /><TextField label="Cor primaria" value={form.primaryColor} onChange={(value) => onChange({ ...form, primaryColor: value })} /><TextField label="Cor secundaria" value={form.secondaryColor} onChange={(value) => onChange({ ...form, secondaryColor: value })} /><TextField label="Cor destaque" value={form.accentColor} onChange={(value) => onChange({ ...form, accentColor: value })} /><Select label="Tema" value={form.themeMode} onChange={(event) => onChange({ ...form, themeMode: event.target.value === "dark" ? "dark" : "light" })}><option value="light">Claro</option><option value="dark">Escuro</option></Select><label className="checkbox"><input type="checkbox" checked={form.isActive} onChange={(event) => onChange({ ...form, isActive: event.target.checked })} /> Ativa</label></FormGrid>;
}
